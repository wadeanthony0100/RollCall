<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wadeanthony0100.rollcall20" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:configChanges="orientation|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".editing_form"
            android:label="New Student Form" >
            <!--stack definition for support android 4.0 and below-->
            <meta-data android:name="android.support.PARENT_ACTIVITY"
                android:value="com.wadeanthony0100.rollcall20.MainActivity"/>
        </activity>
    </application>

</manifest>
